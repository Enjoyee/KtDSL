<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.glimmer.mvvm">

    <application>
        <provider
            android:name=".provider.HammerContentProvider"
            android:authorities="${applicationId}.mvvm-init-provider-glimmer"
            android:exported="false" />
    </application>
</manifest>
